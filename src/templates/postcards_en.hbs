<!DOCTYPE html>
<html lang="en" itemscope itemtype="http://schema.org/CollectionPage">
<head>
    {{seo_en "Postcards" "slide7" description}}
    {{resources false "slide7"}}

    <style>
        :root {
            --page-color: var(--postcards-color);
        }

        .menu a.postcards {
            color: var(--postcards-color);
            opacity: 1;
        }

        .menu > li a.postcards::after {
            width: 100%;
        }

        .cover {
            background-image: url(./assets/pics/slide7.jpg);
            background-position: center 25%;
        }

        @media (pointer: coarse) {
            .menu > li a.postcards::after {
                width: 0;
            }
        }

        @media (max-width: 800px) {
            .cover {
                background-image: url(./assets/pics/slide7-800w.jpg);
            }
        }

        @media (max-width: 400px) {
            .cover {
                background-image: url(./assets/pics/slide7-400w.jpg);
            }
        }
    </style>
</head>
<body>
{{header_en 'postcards'}}
{{collectionArticle_en name description things pictureFolder}}
{{footer_en}}
</body>
</html>
