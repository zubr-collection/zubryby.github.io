<!DOCTYPE html>
<html lang="ru" itemscope itemtype="http://schema.org/CollectionPage">
<head>
    {{seo "Марки" "slide1" description}}
    {{resources false}}

    <style>
        :root {
            --page-color: var(--stamps-color);
        }

        .menu a.stamps {
            color: var(--stamps-color);
            opacity: 1;
        }

        .menu > li a.stamps::after {
            width: 100%;
        }

        .cover {
            background-image: url(./assets/pics/slide1.jpg);
        }

        @media (pointer: coarse) {
            .menu > li a.stamps::after {
                width: 0;
            }
        }

        @media (max-width: 769px) {
            .cover {
                background-image: url(./assets/pics/slide1-800w.jpg);
            }
        }

        @media (max-width: 400px) {
            .cover {
                background-image: url(./assets/pics/slide1-400w.jpg);
            }
        }
    </style>
</head>
<body>
{{header 'stamps'}}
{{collectionArticle name description things pictureFolder}}
{{footer}}
</body>
</html>
