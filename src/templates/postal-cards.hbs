<!DOCTYPE html>
<html lang="ru" itemscope itemtype="http://schema.org/CollectionPage">
<head>
    {{seo "Посткарты" "slide4" description}}
    {{resources false "slide4"}}

    <style>
        :root {
            --page-color: var(--postal-cards-color);
        }

        .menu a.postal-cards {
            color: var(--postal-cards-color);
            opacity: 1;
        }

        .menu > li a.postal-cards::after {
            width: 100%;
        }

        .cover {
            background-image: url(./assets/pics/slide4.jpg);
            background-position: center 25%;
        }

        @media (pointer: coarse) {
            .menu > li a.postal-cards::after {
                width: 0;
            }
        }

        @media (max-width: 800px) {
            .cover {
                background-image: url(./assets/pics/slide4-800w.jpg);
            }
        }

        @media (max-width: 400px) {
            .cover {
                background-image: url(./assets/pics/slide4-400w.jpg);
            }
        }
    </style>
</head>
<body>
{{header 'postal-cards'}}
{{collectionArticle name description things pictureFolder}}
{{footer}}
</body>
</html>
