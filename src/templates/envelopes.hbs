<!DOCTYPE html>
<html lang="ru" itemscope itemtype="http://schema.org/CollectionPage">
<head>
    {{seo "Конверты" "slide4"}}
    {{resources false}}

    <style>
        :root {
            --page-color: var(--envelopes-color);
        }

        .menu a.envelopes {
            color: var(--envelopes-color);
            opacity: 1;
        }

        .menu > li a.envelopes::after {
            width: 100%;
        }

        .cover {
            background-image: url(./assets/pics/slide6.jpg);
            background-position: center 25%;
        }

        @media (pointer: coarse) {
            .menu > li a.envelopes::after {
                width: 0;
            }
        }

        @media (max-width: 769px) {
            .cover {
                background-image: url(./assets/pics/slide6-800w.jpg);
            }
        }

        @media (max-width: 400px) {
            .cover {
                background-image: url(./assets/pics/slide6-400w.jpg);
            }
        }
    </style>
</head>
<body>
{{header}}
<div class="cover"></div>
<article itemscope itemtype="http://schema.org/Collection" itemid="https://zubry.by/envelopes.html">
    <div>
        <h2 itemprop="name">{{name}}</h2>
        <p itemprop="description">{{description}}</p>
        {{#each things}}
            <section itemscope itemtype="http://schema.org/Thing">
                <picture>
                    <source media="(max-width: 480px)" srcset="./assets/pics/{{../pictureFolder}}/{{image}}-480w.jpg">
                    <img itemprop="image"
                         src="./assets/pics/{{../pictureFolder}}/{{image}}.jpg"
                         width="400" height="200" alt="Изображение марок" />
                </picture>
                <div class="details">
                    <time itemprop="description">{{year}}</time>
                    <h3 itemprop="name">{{title}}</h3>
                    <dl itemprop="description">
                        {{#each props}}
                            <dt>{{name}}</dt>
                            <dd>{{value}}</dd>
                        {{/each}}
                    </dl>
                </div>
                <link itemprop="isPartOf" itemscope itemtype="http://schema.org/Collection" itemid="https://zubry.by/envelopes.html" />
            </section>
        {{/each}}
    </div>
</article>
{{footer}}
</body>
</html>
