<!DOCTYPE html>
<html lang="ru" itemscope itemtype="http://schema.org/CollectionPage">
<head>
    {{seo "Конверты" "slide4" description}}
    {{resources false}}

    <style>
        :root {
            --page-color: var(--envelopes-color);
        }

        .menu a.envelopes {
            color: var(--envelopes-color);
            opacity: 1;
        }

        .menu > li a.envelopes::after {
            width: 100%;
        }

        .cover {
            background-image: url(./assets/pics/slide6.jpg);
            background-position: center 25%;
        }

        @media (pointer: coarse) {
            .menu > li a.envelopes::after {
                width: 0;
            }
        }

        @media (max-width: 769px) {
            .cover {
                background-image: url(./assets/pics/slide6-800w.jpg);
            }
        }

        @media (max-width: 400px) {
            .cover {
                background-image: url(./assets/pics/slide6-400w.jpg);
            }
        }
    </style>
</head>
<body>
{{header}}
{{collectionArticle name description things pictureFolder}}
{{footer}}
</body>
</html>
